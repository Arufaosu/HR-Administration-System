# For Discord authentication (if you want to use Discord authentication with NextAuth)
AUTH_DISCORD_ID=your-discord-client-id
AUTH_DISCORD_SECRET=your-discord-client-secret

# For database connection (SQLite for now, since you're using Prisma with SQLite)
DATABASE_URL="file:./dev.db"

